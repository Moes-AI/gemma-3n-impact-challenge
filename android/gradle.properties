# Increase the Gradle daemon's heap size to 4GB to handle large assets.
org.gradle.jvmargs=-Xmx4g

# Explicitly enable Hermes, as the build script is failing to find this property.
hermesEnabled=true

# Enable AndroidX, required by modern React Native dependencies.
android.useAndroidX=true
